{% extends 'base.html.twig' %}

{% block title %}Edit Country{% endblock %}

{% block body %}
    <div class="d-flex flex-column align-items-center w-75 mx-auto">
        <h1>Edit Country</h1>

        {{ include('country/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('country/_delete_form.html.twig') }}

        <a class="btn btn-primary mt-3" href="{{ path('app_country_index') }}">Back to list of countries</a>
    </div>
{% endblock %}
