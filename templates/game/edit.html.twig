{% extends 'base.html.twig' %}

{% block title %}Edit Game{% endblock %}

{% block body %}
    <div class="d-flex flex-column align-items-center w-75 mx-auto">
        <h1>Edit Game</h1>

        {{ include('game/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('game/_delete_form.html.twig') }}

        <a class="btn btn-primary mt-3" href="{{ path('app_game_index') }}">Back to list of games</a>
    </div>
{% endblock %}
