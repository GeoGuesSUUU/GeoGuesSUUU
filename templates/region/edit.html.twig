{% extends 'base.html.twig' %}

{% block title %}Edit Region{% endblock %}

{% block body %}
    <div class="d-flex flex-column align-items-center w-75 mx-auto">
        <h1>Edit Region</h1>

        {{ include('region/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('region/_delete_form.html.twig') }}

        <a class="btn btn-primary mt-3" href="{{ path('app_region_index') }}">Back to list of regions</a>
    </div>
{% endblock %}
