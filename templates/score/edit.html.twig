{% extends 'base.html.twig' %}

{% block title %}Edit Score{% endblock %}

{% block body %}
    <div class="d-flex flex-column align-items-center w-75 mx-auto">

        <h1>Edit Score</h1>

        {{ include('score/_form.html.twig', {'button_label': 'Update'}) }}
        {{ include('score/_delete_form.html.twig') }}

        <a class="btn btn-primary mt-3" href="{{ path('app_score_index') }}">Back to list of scores</a>

    </div>
{% endblock %}
